/**
 * Archivo: App.tsx
 * Sirve para: definir el componente raíz que se renderiza en pantalla.
 * Consejo: cuando pruebes diseño o estructura, empieza por App y luego divide en componentes.
 */

import './App.css'

import { MainLayout } from './layout/MainLayout';

function App() {

  return (
    <MainLayout /> 
  );
}

export default App