/**
 * Archivo: BackendLayout.tsx
 * Sirve para: reservar un layout para una zona interna/administración (backend).
 * Consejo: cuando lo implementes, reutiliza componentes comunes para no duplicar código.
 */
